[Unit]
Description=Hop Launcher Daemon (hopd)
Wants=graphical-session.target
After=graphical-session.target dbus.service

[Service]
Type=simple
ExecStart=/usr/bin/hopd --socket %t/hopd.sock
Restart=on-failure
RestartSec=1
Environment=HOPD_SOCKET=%t/hopd.sock

[Install]
WantedBy=default.target
