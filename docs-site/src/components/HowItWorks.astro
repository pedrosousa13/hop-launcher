<section class="mx-auto mt-18 w-full max-w-6xl px-6 md:px-10">
  <div class="surface-card rounded-3xl p-6 md:p-8">
    <h2 class="font-display text-3xl font-semibold md:text-4xl">How It Works</h2>
    <div class="mt-6 grid gap-6 md:grid-cols-3">
      <article>
        <h3 class="text-base font-semibold">Daemon Core</h3>
        <p class="mt-2 text-sm leading-relaxed text-[color:var(--muted)]">
          <code class="font-mono text-xs">hopd</code> owns launcher IPC and serves the frontend with deterministic request/response behavior.
        </p>
      </article>
      <article>
        <h3 class="text-base font-semibold">Hotkey Bridge</h3>
        <p class="mt-2 text-sm leading-relaxed text-[color:var(--muted)]">
          <code class="font-mono text-xs">hop-hotkeyd</code> routes toggle events across X11 and Wayland compositor-specific paths with health reporting.
        </p>
      </article>
      <article>
        <h3 class="text-base font-semibold">Client Frontend</h3>
        <p class="mt-2 text-sm leading-relaxed text-[color:var(--muted)]">
          The GTK launcher client focuses on responsive keyboard flow while the daemons handle control-plane orchestration.
        </p>
      </article>
    </div>
  </div>
</section>
