<section id="status" class="mx-auto mt-18 mb-20 w-full max-w-6xl px-6 md:px-10">
  <div class="surface-card grid gap-5 rounded-3xl p-6 md:grid-cols-[1.45fr_1fr] md:items-center md:p-8">
    <div>
      <p class="text-xs font-semibold tracking-[0.16em] text-[color:var(--muted)] uppercase">Status</p>
      <h2 class="mt-3 font-display text-3xl font-semibold md:text-4xl">Cross-Linux rollout with managed release channels</h2>
      <p class="mt-4 max-w-2xl text-sm leading-relaxed text-[color:var(--muted)] md:text-base">
        The project ships daemon and launcher artifacts, AppImage packaging, and distro publish scaffolding with verification runbooks.
      </p>
    </div>
    <div class="surface-soft rounded-2xl p-5">
      <h3 class="text-sm font-semibold">Developer Footnotes</h3>
      <ul class="mt-3 space-y-2 text-sm text-[color:var(--muted)]">
        <li><code class="font-mono text-xs">crates/hopd</code> daemon runtime</li>
        <li><code class="font-mono text-xs">crates/hop-hotkeyd</code> shortcut bridge</li>
        <li><code class="font-mono text-xs">apps/gtk-launcher</code> desktop client</li>
      </ul>
    </div>
  </div>
</section>
